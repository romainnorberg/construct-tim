imports:
    - { resource: config_dev.yml }
    - { resource: parameters_test.php }

framework:
    test: ~
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false

web_profiler:
    toolbar: false
    intercept_redirects: false

doctrine:
    dbal:
        driver:   pdo_mysql
        host:     127.0.0.1
        dbname:   constructtim
        user:     travis
        password: ''
        charset:  UTF8
